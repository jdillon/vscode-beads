node_modules/
dist/
*.vsix
.vscode-test/
*.log
.DS_Store
tmp/

# Local dev artifacts (code-server, openvscode-server)
.cache/
.openvscode-server/
.local/
.beads/

# Local environment notes (not committed)
CLAUDE.local.md

# opencode is not used in this project; the CLI recreates these on any run
# (it rewrites package.json on plugin autoupdate), so keep them out of git
.opencode/
opencode.json
