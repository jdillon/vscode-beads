node_modules/
dist/
*.vsix
.vscode-test/
*.log
.DS_Store
tmp/

# Local dev artifacts (code-server, openvscode-server)
.cache/
.openvscode-server/
.local/
.beads/

# Local environment notes (not committed)
CLAUDE.local.md

# opencode is not used in this project. Its CLI recreates these paths on any run
# and rewrites .opencode/package.json whenever the plugin autoupdates, so keep
# them out of git. The repo's own root package.json is unaffected and tracked.
.opencode/
opencode.json
