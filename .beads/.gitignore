# SQLite databases
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Protected branch mode: issue data goes to beads-metadata branch via worktree
# Main branch only keeps config files, not issue data
beads.jsonl
issues.jsonl
deletions.jsonl

# Keep config files on main
!metadata.json
!config.json
